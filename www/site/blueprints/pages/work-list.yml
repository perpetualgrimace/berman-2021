title: Work Directory
preset: page
icon: briefcase
pages:
  sortable: true
  template: work
options:
  changeTemplate: true

fields:
  headline:
    extends: fields/headline
    width: 1
  heroImg:
    extends: fields/heroImg
    width: 1
  # socialImg: fields/socialImg

  # featuredWork:
  #   label: Home page featured work
  #   type: structure
  #   style: table
  #   fields:
  #     images:
  #       label: image
  #       type: files
  #     client:
  #       label: client name
  #       required: true
  #     caption:
  #       label: caption
  #     pages:
  #       label: link
  #       type: select
  #       options: children

  # ctaLine: fields/ctaLine
  # ctaHelp: fields/ctaHelp
  # cta: fields/cta
  # ctaButton: fields/ctaButton
  # ctaLink: fields/ctaLink
